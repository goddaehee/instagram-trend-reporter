# Streamlit Secrets 예제
# 이 파일을 복사해서 secrets.toml로 저장하거나
# Streamlit Cloud의 Secrets 설정에서 입력하세요

# Apify API 토큰
APIFY_TOKEN = "apify_api_xxx"

# Google OAuth
GOOGLE_CLIENT_ID = "xxx.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET = "GOCSPX-xxx"

# Google Sheets 토큰 (JSON 문자열)
# 로컬에서 실행 후 아래 명령어로 추출:
# python3 -c "import keyring; print(keyring.get_password('agent-skills', 'google-sheets-token-json'))"
SHEETS_TOKEN = '{"token": "...", "refresh_token": "...", ...}'

# Gmail 토큰 (JSON 문자열)
# python3 -c "import keyring; print(keyring.get_password('agent-skills', 'gmail-token-json'))"
GMAIL_TOKEN = '{"token": "...", "refresh_token": "...", ...}'
